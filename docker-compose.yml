# Docker Compose configuration for my-project-terra
services:
  backend:
    build: ./backend
    image: my-project-terra-backend:latest
    ports:
      - "3000:3000"

  frontend:
    build: ./frontend
    image: my-project-terra-frontend:latest
    ports:
      - "8080:80"